--- # YAML TABLE RULES
table-name: "" #name of the table in this doc
required-tables: #list of table names for required tables if rolling on other tables
	- "" 
	- "" 
	- "" 
roll: "" #die roll notation to roll on the table, alternativly 'length' will roll 1d<length>
result: # List result that the dice could produce
	1: "" # string as result
	
	2: 	#To roll on nested table
		roll: "" #die roll notation to roll on the table, alternativly 'length' will roll 1d<length>
		result:	# list result that the dice could produce
			1: "" # string as result
			2: ""
	
	3:	# To roll on another table
		roll-on: "" #name of table to roll on.
		roll-amount: "" # OPTIONAL roll notation to roll on outside table. if empty, will default to outside table's roll.
	
	4: # results can also be a list of items
	
	
	Item Notation
	name:	# item name
	amount: # can be digit or die notation
	multiplier: # OPTIONAL - multiply amount by this digit
	
	if result == string
		return string
	
	if result == dict and contains 'name' and 'amount'
		return 'amount' * 1 if no 'multiplier' else 'amount' * 'multiplier' with 'name'
				
	if result == dict and contains 'roll-on'
		roll on named table by 'roll-amount' if provided else 'roll-amount' will be default to the table
		
	if result == list
		test all cases for each item
		